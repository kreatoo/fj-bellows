version: "2"
run:
  concurrency: 4
  timeout: 5m
  tests: true

issues:
  max-issues-per-linter: 0
  max-same-issues: 50

linters:
  exclusions:
    # These exclusions cover deliberate compatibility/adapter shapes and
    # existing test fixtures. Keep the checks enabled for the rest of the tree.
    rules:
      - path: internal/orchestrator/orchestrator.go
        linters: [containedctx, nestif]
      - path: internal/orchestrator/orchestrator_test.go
        linters: [goconst, revive]
      - path: internal/provider/digitalocean/client.go
        linters: [dupl]
      - path: internal/provider/digitalocean/firewall.go
        linters: [goconst, gocyclo, revive]
      - path: internal/provider/digitalocean/provision.go
        linters: [goconst, gocritic]
      - path: internal/provider/digitalocean/.*_test.go
        linters: [goconst]
      - path: internal/provider/digitalocean/(destroy|digitalocean|list|names|provision).go
        linters: [revive]
  enable:
    # correctness
    - govet
    - staticcheck
    - unused
    - errcheck
    - errorlint
    - nilerr
    - forcetypeassert
    - copyloopvar
    - durationcheck
    - reassign
    # style
    - gocritic
    - revive
    - whitespace
    - godot
    - misspell
    - dupword
    - predeclared
    - errname
    - asciicheck
    # complexity
    - gocyclo
    - nestif
    - funlen
    - dupl
    # performance
    - perfsprint
    - unconvert
    - ineffassign
    - goconst
    # security & resources
    - gosec
    - bidichk
    - bodyclose
    - noctx
    - containedctx
    - fatcontext
    # logging
    - sloglint
    - loggercheck
    # testing
    - thelper
    - usetesting
    # modernization & meta
    - modernize
    - exptostd
    - intrange
    - usestdlibvars
    - exhaustive
    - nolintlint
  disable:
    # wsl_v5: whitespace/blank-line opinions are too noisy for this codebase.
    - wsl_v5
    # paralleltest: conflicts with t.Setenv-based tests and is opinionated.
    - paralleltest

  settings:
    dupl:
      threshold: 100
    errcheck:
      check-type-assertions: true
    funlen:
      lines: 120
      statements: 80
    goconst:
      min-len: 3
      min-occurrences: 4
    gocyclo:
      # Bumped from 13 → 15 alongside FJB-94 (each phase adds one
      # top-level setup branch to cmd/fj-bellows main.run). 15 still
      # catches sprawling functions (upstream default is 30) without
      # making every cross-cutting concern an extract-a-helper cycle.
      min-complexity: 15
    nolintlint:
      require-explanation: true
      require-specific: true

formatters:
  enable:
    - gofmt
    - gofumpt
  settings:
    gofumpt:
      extra-rules: true
